spring:
  application:
    name: microservices_sandbox
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/microservices_sandbox?useTimezone=true&serverTimezone=UTC
    username: root
    password: '123456789'
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        #dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
    open-in-view: false
logging:
  level:
    root:
    br.com.lima: DEBUG
